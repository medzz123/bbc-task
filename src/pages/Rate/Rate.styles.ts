import { styled } from '@theme/config';

export const RateContainer = styled('main', {
  flexDirection: 'column',
});

export const RateContent = styled('div', {
  padding: '$20 $20 $60 $20',
  maxWidth: '$5xl',
  margin: '0 auto',
  width: '$full',
});
